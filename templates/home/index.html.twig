<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to Comms</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body {
      background: #f9f5ff;
      font-family: 'Inter', 'Segoe UI', sans-serif;
      margin: 0;
      padding: 0;
    }

    /* Your Original Navbar Styles */
    .navbar {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      background: transparent;
      box-shadow: none;
      z-index: 1000;
      transition: background-color 0.4s ease, box-shadow 0.4s ease;
      padding: 1rem 2rem;
    }

    .navbar a.nav-link,
    .navbar .navbar-brand {
      color: white !important;
      font-weight: 500;
      transition: color 0.3s ease;
      text-decoration: none;
    }

    .navbar a.nav-link:hover {
      color: #f3e8ff !important;
    }

    .navbar.scrolled {
      background-color: white;
      box-shadow: 0 2px 15px rgba(0,0,0,0.1);
    }

    .navbar.scrolled a.nav-link,
    .navbar.scrolled .navbar-brand {
      color: #4b0082 !important;
    }

    .navbar .flex-1 {
      flex: 1;
    }

    .navbar .flex-none {
      display: flex;
      align-items: center;
      gap: 1.5rem;
    }

    .input-search {
      background: rgba(255,255,255,0.95) !important;
      border: 1px solid rgba(0,0,0,0.06) !important;
      border-radius: 10px !important;
      padding: 0.55rem 0.9rem !important;
      width: 220px !important;
      box-shadow: 0 2px 6px rgba(0,0,0,0.06) !important;
    }

    .btn-ghost {
      background: transparent;
      border: none;
      padding: 0.5rem;
      cursor: pointer;
    }

    .btn-circle {
      border-radius: 50%;
    }

    .avatar img {
      border: 2px solid rgba(255,255,255,0.9) !important;
      border-radius: 50%;
      width: 40px;
      height: 40px;
      object-fit: cover;
      cursor: pointer;
      transition: transform 0.3s ease;
    }

    .navbar.scrolled .avatar img {
      border-color: #7c3aed !important;
    }

    .avatar img:hover {
      transform: scale(1.1);
    }

    .dropdown {
      position: relative;
    }

    .dropdown-content {
      display: none;
      position: absolute;
      top: calc(100% + 8px);
      right: 0;
      background: white;
      border-radius: 12px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.15);
      padding: 0.5rem;
      min-width: 200px;
      z-index: 1001;
    }

    .dropdown-content.show {
      display: block;
    }

    .dropdown-content li {
      list-style: none;
    }

    .dropdown-content a {
      display: flex;
      align-items: center;
      gap: 0.8rem;
      padding: 0.7rem 1rem;
      color: #333 !important;
      text-decoration: none;
      border-radius: 8px;
      transition: all 0.2s ease;
    }

    .dropdown-content a:hover {
      background: #f9f5ff;
      color: #7c3aed !important;
    }

    /* Hero Section */
    .hero {
      text-align: center;
      padding: 10rem 2rem 5rem;
      background: linear-gradient(135deg, #ebd3feff, #c084fc);
      color: white;
      border-radius: 0 0 50px 50px;
      box-shadow: 0 12px 25px rgba(0, 0, 0, 0.1);
      position: relative;
      overflow: hidden;
    }

    .hero::before {
      content: "";
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle at 50% 20%, rgba(255,255,255,0.2), transparent 60%);
      z-index: 0;
    }

    .hero h1 {
      position: relative;
      font-size: 3.5rem;
      font-weight: 800;
      margin-bottom: 1rem;
      letter-spacing: 0.5px;
      z-index: 1;
      animation: fadeInUp 0.8s ease-out;
    }

    .hero h1 span {
      color: #f3e8ff;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
    }

    .hero p {
      position: relative;
      font-size: 1.2rem;
      max-width: 700px;
      margin: 0 auto 2rem;
      opacity: 0.95;
      line-height: 1.8;
      z-index: 1;
      animation: fadeInUp 0.8s ease-out 0.2s both;
    }

    .hero .btn-primary {
      position: relative;
      background-color: white;
      color: #7c3aed;
      font-weight: 700;
      border: none;
      border-radius: 12px;
      padding: 1rem 2.5rem;
      transition: all 0.3s ease;
      box-shadow: 0 4px 15px rgba(255,255,255,0.3);
      text-decoration: none;
      display: inline-block;
      z-index: 1;
      font-size: 1.1rem;
      animation: fadeInUp 0.8s ease-out 0.4s both;
      cursor: pointer;
    }

    .hero .btn-primary:hover {
      background-color: #f3e8ff;
      transform: translateY(-3px);
      box-shadow: 0 6px 20px rgba(255,255,255,0.4);
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Features Section */
    .features {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2.5rem;
      max-width: 1100px;
      margin: 5rem auto;
      padding: 0 2rem;
    }

    .feature-card {
      background-color: white;
      border-radius: 20px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.08);
      padding: 2.5rem 2rem;
      text-align: center;
      transition: all 0.4s ease;
      position: relative;
      overflow: hidden;
    }

    .feature-card::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 5px;
      background: linear-gradient(90deg, #7c3aed, #c084fc);
      transform: scaleX(0);
      transition: transform 0.4s ease;
    }

    .feature-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 15px 35px rgba(124, 58, 237, 0.15);
    }

    .feature-card:hover::before {
      transform: scaleX(1);
    }

    .feature-card .icon-wrapper {
      width: 80px;
      height: 80px;
      margin: 0 auto 1.5rem;
      background: linear-gradient(135deg, #ebd3feff, #c084fc);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.4s ease;
    }

    .feature-card:hover .icon-wrapper {
      transform: scale(1.1) rotate(5deg);
    }

    .feature-card img {
      width: 45px;
      height: 45px;
      filter: brightness(0) invert(1);
    }

    .feature-card h3 {
      color: #4b0082;
      margin-bottom: 1rem;
      font-size: 1.4rem;
      font-weight: 700;
    }

    .feature-card p {
      color: #666;
      font-size: 1rem;
      line-height: 1.6;
    }

    /* Statistics Section */
    .stats {
      background: linear-gradient(135deg, #7c3aed, #a855f7);
      color: white;
      padding: 4rem 2rem;
      margin: 4rem 0;
      text-align: center;
    }

    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 3rem;
      max-width: 1000px;
      margin: 0 auto;
    }

    .stat-item {
      padding: 1.5rem;
    }

    .stat-number {
      font-size: 3rem;
      font-weight: 800;
      margin-bottom: 0.5rem;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
    }

    .stat-label {
      font-size: 1.1rem;
      opacity: 0.95;
      font-weight: 500;
    }

    /* CTA Section */
    .cta {
      text-align: center;
      margin: 5rem auto 4rem;
      padding: 0 2rem;
      max-width: 800px;
    }

    .cta h2 {
      font-size: 2.5rem;
      font-weight: 800;
      color: #4b0082;
      margin-bottom: 1.5rem;
    }

    .cta p {
      font-size: 1.2rem;
      color: #666;
      margin-bottom: 2.5rem;
      line-height: 1.6;
    }

    .cta .btn {
      background: linear-gradient(135deg, #8b5cf6, #c084fc);
      color: white;
      padding: 1rem 3rem;
      border-radius: 12px;
      text-decoration: none;
      font-weight: 700;
      font-size: 1.1rem;
      transition: all 0.3s ease;
      display: inline-block;
      box-shadow: 0 4px 15px rgba(139, 92, 246, 0.3);
      cursor: pointer;
    }

    .cta .btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 25px rgba(139, 92, 246, 0.4);
      background: linear-gradient(135deg, #7c3aed, #a855f7);
    }

    /* Responsive */
    @media (max-width: 768px) {
      .navbar .flex-none {
        gap: 1rem;
      }

      .input-search {
        width: 150px !important;
      }

      .hero {
        padding: 8rem 2rem 4rem;
      }

      .hero h1 {
        font-size: 2.5rem;
      }
      
      .hero p {
        font-size: 1rem;
      }

      .features {
        gap: 2rem;
        margin: 3rem auto;
      }

      .cta h2 {
        font-size: 2rem;
      }
    }
  </style>
</head>
<body>
  <!-- Your Original Navbar -->
  <header>
    <nav class="navbar" id="navbar" style="display: flex; justify-content: space-between; align-items: center;">
      <div class="flex-1">
        <a class="btn btn-ghost" href="#home">
          <img src="{{ asset('Primary Logo.png') }}" alt="ArtComms Logo" style="height:40px; object-fit: contain;">
        </a>
      </div>

      <div class="flex-none items-center" style="display: flex; align-items: center; gap: 1.5rem;">
        <input type="text" placeholder="Search" class="input-search"/>
        
        <div class="dropdown dropdown-end" style="position:relative;">
          <div tabindex="0" role="button" class="btn btn-ghost btn-circle avatar" onclick="toggleDropdown()">
            <img src="{{ asset('PROFILE.png') }}" alt="User Avatar">
          </div>
          <ul id="avatar-dropdown" class="dropdown-content">
            <a href="{{ path('app_sample') }}"><i class="bi bi-person-circle me-2"></i>Account</a>
            <a href="{{ path('app_commission_index') }}"><i class="bi bi-palette me-2"></i>Commissions</a>
            <a href="{{ path('app_admin_dashboard') }}"><i class="bi bi-speedometer2 me-2"></i>Admin Dashboard</a>
            <li><a href="#settings"><i class="bi bi-gear me-2"></i>Settings</a></li>
            <a href="{{ path('app_logout') }}"><i class="bi bi-box-arrow-right me-2"></i>Logout</a>
          </ul>
        </div>
      </div>
    </nav>
  </header>

  <!-- Hero Section -->
  <div class="hero">
    <h1>Welcome to <span>Comms</span></h1>
    <p style="font-size: 1.3rem; font-weight: 600; margin-bottom: 1rem;">Where talent meets opportunity.</p>
    <p>Artists and clients meet for creative collaboration. Discover talented artists, request commissions, and bring your ideas to life.</p>
    <a href="#explore" class="btn-primary">Explore Commissions</a>
  </div>

  <!-- Statistics Section -->
  <section class="stats">
    <div class="stats-grid">
      <div class="stat-item">
        <div class="stat-number">500+</div>
        <div class="stat-label">Active Artists</div>
      </div>
      <div class="stat-item">
        <div class="stat-number">2,000+</div>
        <div class="stat-label">Commissions Completed</div>
      </div>
      <div class="stat-item">
        <div class="stat-number">98%</div>
        <div class="stat-label">Client Satisfaction</div>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section class="features">
    <div class="feature-card">
      <div class="icon-wrapper">
        <svg width="45" height="45" viewBox="0 0 24 24" fill="currentColor">
          <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8zm-5.5-2.5l7.51-3.49L17.5 6.5 9.99 9.99 6.5 17.5zm5.5-6.6c.61 0 1.1.49 1.1 1.1s-.49 1.1-1.1 1.1-1.1-.49-1.1-1.1.49-1.1 1.1-1.1z"/>
        </svg>
      </div>
      <h3>Creative Freedom</h3>
      <p>Artists express their unique styles while clients get custom, one-of-a-kind pieces tailored to their vision.</p>
    </div>

    <div class="feature-card">
      <div class="icon-wrapper">
        <svg width="45" height="45" viewBox="0 0 24 24" fill="currentColor">
          <path d="M18 8h-1V6c0-2.76-2.24-5-5-5S7 3.24 7 6v2H6c-1.1 0-2 .9-2 2v10c0 1.1.9 2 2 2h12c1.1 0 2-.9 2-2V10c0-1.1-.9-2-2-2zM9 6c0-1.66 1.34-3 3-3s3 1.34 3 3v2H9V6zm9 14H6V10h12v10zm-6-3c1.1 0 2-.9 2-2s-.9-2-2-2-2 .9-2 2 .9 2 2 2z"/>
        </svg>
      </div>
      <h3>Safe & Secure</h3>
      <p>All commissions are managed transparently with fair pricing, secure transactions, and clear communication.</p>
    </div>

    <div class="feature-card">
      <div class="icon-wrapper">
        <svg width="45" height="45" viewBox="0 0 24 24" fill="currentColor">
          <path d="M16 11c1.66 0 2.99-1.34 2.99-3S17.66 5 16 5c-1.66 0-3 1.34-3 3s1.34 3 3 3zm-8 0c1.66 0 2.99-1.34 2.99-3S9.66 5 8 5C6.34 5 5 6.34 5 8s1.34 3 3 3zm0 2c-2.33 0-7 1.17-7 3.5V19h14v-2.5c0-2.33-4.67-3.5-7-3.5zm8 0c-.29 0-.62.02-.97.05 1.16.84 1.97 1.97 1.97 3.45V19h6v-2.5c0-2.33-4.67-3.5-7-3.5z"/>
        </svg>
      </div>
      <h3>Vibrant Community</h3>
      <p>Join a network of passionate creators and supporters sharing artistic visions and inspiring each other.</p>
    </div>
  </section>

  <!-- Call to Action -->
  <section class="cta">
    <h2>Ready to Start Your Next Project?</h2>
    <p>Find the perfect artist for your vision or start accepting commissions today and turn your creative dreams into reality.</p>
    <a href="#create" class="btn">Create a Commission</a>
  </section>

  <script>
    // Your original dropdown toggle
    function toggleDropdown() {
      const dropdown = document.getElementById('avatar-dropdown');
      dropdown.classList.toggle('show');
    }

    // Close dropdown when clicking outside
    document.addEventListener('click', function(event) {
      const avatarBtn = document.querySelector('.avatar');
      const dropdown = document.getElementById('avatar-dropdown');
      if (!avatarBtn.contains(event.target) && !dropdown.contains(event.target)) {
        dropdown.classList.remove('show');
      }
    });
    
    // Your original navbar scroll effect
    window.addEventListener('scroll', function() {
      const navbar = document.querySelector('.navbar');
      if (window.scrollY > 50) {
        navbar.classList.add('scrolled');
      } else {
        navbar.classList.remove('scrolled');
      }
    });
  </script>
</body>
</html>